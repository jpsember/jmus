enum ChordType;
enum Accidental;
enum OptType;

fields {

  Accidental accidental;
  ChordType type;
  OptType opt_type;

  int number;

  // -1 indicates it occupies every beat in the measure
  //
  int beat = -1;

  ?Chord slash_chord;
}
