enum ChordType;
enum Accidental;
enum OptType;

fields {

  Accidental accidental;
  ChordType type;
  OptType opt_type;

  int number;

  // If >= 0, it is part of a group of chords to be displayed within a single bar
  //
  int beat_number = -1;

  ?Chord slash_chord;
}
